
# 代码生成
gen: 
  # 作者
  author: ymsw
  # 默认生成包路径 mapper.system 需改成自己的模块名称 如 mapper.system monitor tool
  packageName: com.ymsw.mapper.system
  # 自动去除表前缀，默认是false
  autoRemovePre: false
  # 表前缀（生成类名不会包含表前缀，多个用逗号分隔）
  tablePrefix: sys_